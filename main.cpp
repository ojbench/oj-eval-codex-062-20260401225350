#include <bits/stdc++.h>
#include MemoryRiver.hpp

using namespace std;

// This main is only to satisfy the build; the OJ for this task
// focuses on MemoryRiver.hpp behavior via its own tests.
// We will implement a simple no-op main.

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    // No I/O required; just exit success.
    return 0;
}

